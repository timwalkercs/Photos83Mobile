<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidphotos83">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidPhotos83">
        <activity
            android:name=".SlideshowView"
            android:exported="false"
            android:label="@string/title_activity_slideshow_view"
            android:theme="@style/Theme.AndroidPhotos83.NoActionBar" />
        <activity
            android:name=".AlbumView"
            android:exported="false" />
        <activity
            android:name=".PhotoView"
            android:exported="false" />
        <activity
            android:name=".AlbumList"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>